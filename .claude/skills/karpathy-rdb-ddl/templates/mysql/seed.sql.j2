-- Seed for {{ entity.schema }}.{{ entity.table }} (preset, mysql)
INSERT IGNORE INTO `{{ entity.schema }}`.`{{ entity.table }}` ({{ columns | join(', ') }}) VALUES
{%- for row in rows %}
    ({{ row | join(', ') }}){% if not loop.last %},{% else %};{% endif %}
{%- endfor %}
