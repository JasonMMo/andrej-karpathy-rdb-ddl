-- V003 Generated by andrej-karpathy-rdb-ddl (dialect: mysql)
-- NOTE: MySQL < 8.0.29 does not support "CREATE INDEX IF NOT EXISTS".
-- If targeting older MySQL, run migrations with idempotent runner (e.g. Flyway versioned scripts).
{% for idx in indexes %}
CREATE {% if idx.unique %}UNIQUE {% endif %}INDEX `{{ idx.name }}` ON `{{ idx.schema }}`.`{{ idx.table }}` ({{ idx.columns | join(', ') }});
{% endfor %}
